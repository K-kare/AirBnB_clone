The project: AirBnB clone - The console
To start the console,,Execute the console.py file
The user enters command to the console which are interpreted 
For example:
when you input the exit command the console terminates
